package com.learnnow.privacy.entity;

/** What a grievance is about. Mirrored by a CHECK constraint in V2. */
public enum GrievanceCategory {
    DATA_ACCESS,
    DATA_CORRECTION,
    DATA_ERASURE,
    CONSENT,
    SECURITY,
    OTHER
}
